# Copy to .env and fill your local credentials

# Option A: Use individual keys and select with --env-key
INVENTORY_DB=postgresql://USER:PASSWORD@localhost:5432/inventory_db_01
IAM_DB=postgresql://USER:PASSWORD@localhost:5432/iam_db

# Option B: Set a single DSN consumed by PG_DSN or --dsn overrides
# PG_DSN=postgresql://USER:PASSWORD@localhost:5432/your_database
